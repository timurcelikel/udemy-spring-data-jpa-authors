# Use bookuser for all ITs
spring.datasource.username=bookuser
spring.datasource.password=password

spring.datasource.url=jdbc:mysql://localhost:3306/bookdb?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC

# We were using this when we had schema.sql in /resources. Now that we are using liquibase I'm not sure this is needed
spring.sql.init.mode=always

# Use bookadmin user with elevated privileges for liquibase actions
spring.liquibase.user=bookadmin
spring.liquibase.password=password
